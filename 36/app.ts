// Declare the function

function make_shirt(size:string , text:string ) {
    console.log(`your shirt size is ${size}, and the msg printed on that is ${text}`);
    }

    // call the function

make_shirt( 'small', `"Hello Kitty!"`);
